/** PostgREST table names (lowercase, unquoted — matches `public.linkups` in Postgres). */
export const LINKUPS_TABLE = "linkups" as const;
export const LINKUP_ATTENDEES_TABLE = "linkup_attendees" as const;
